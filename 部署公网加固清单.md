# BabyLog 公网部署加固清单

> 适用：把 BabyLog 部署到有公网 IP 的云服务器。
> 按编号顺序执行，每完成一项打勾。动手前先备份：`cp -r <项目路径> <项目路径>_backup`
>
> 涉及文件：config.py、app/__init__.py、app/auth.py、run.py、register.py，部署时新增 systemd / nginx 配置

---

## 现状核对

| 检查点 | 当前代码状态 | 风险 |
|--------|-------------|------|
| Web 服务器 | `run.py` 用 Flask 开发服务器 `app.run()` | 高危 |
| HTTPS | 无，cookie `secure=False` 明文传输 | 高危 |
| 管理员密码 | 预置 `admin / admin` | 高危 |
| SECRET_KEY | `app/__init__.py` 每次启动 `secrets.token_hex(32)` 随机生成 | 中 |
| 登录限速 | 无 | 高危 |
| 注册接口 | `/api/register` 完全开放 | 中 |
| 数据库备份 | 无 | 中 |
| CSRF 防护 | 无 | 中 |

---

## 第 1 步：创建管理员（必做）

新部署的数据库没有预置账号，先手动创建第一个管理员（用强密码，别用 admin / admin123 这类已公开的弱密码）：

```bash
cd <项目路径> && python register.py 你的用户名 你的强密码 --admin
```

已有 admin、只想改密码的话，在服务器上执行（把 `新密码` 换成你的）：

```bash
cd <项目路径> && python3 - <<'EOF'
from app import create_app, db, migrate_schema
from app.models import User
from app.auth import hash_password
app = create_app()
with app.app_context():
    migrate_schema(app)
    u = User.query.filter_by(username='admin').first()
    if u:
        u.password_hash = hash_password('新密码')
        db.session.commit()
        print('[✓] admin 密码已更新')
    else:
        print('[!] 未找到 admin 用户')
EOF
```

验证：用新密码能登录、旧密码失效。

---

## 第 2 步：固定 SECRET_KEY（部署时重新生成）

代码已改为从 config.py 读固定 SECRET_KEY（不再每次重启随机），重启不掉线、多 worker 会话共享。但 config.py 里的默认值仅供本地开发，公网部署必须换新的：

```bash
python3 -c "import secrets; print(secrets.token_hex(32))"
```

推荐用环境变量设置（不写进 git）。用 systemd 托管时，在 `/etc/systemd/system/babylog.service` 的 `[Service]` 段加：

```ini
Environment=SECRET_KEY=粘贴上一步生成的值
```

验证：重启后已登录用户不掉线即生效。迁移服务器时：沿用同一个 key 可保留登录态；想轮换就重新生成（用户需重新登录一次）。

---

## 第 3 步：登录限速（防爆破，已内置）

代码已实现数据库持久化的登录限速：

- 同一「用户名 + IP」连续失败 5 次，锁定 15 分钟，期间返回 429
- 记录存在 `login_attempts` 表，重启后依然有效
- 登录成功清零；距上次失败超 15 分钟自动重新计数

实现位置：app/models.py（LoginAttempt）+ app/auth.py（_is_locked / _record_fail / _clear_fails）。

验证：连续输错 6 次，第 6 次起返回 429；正确登录一次后计数清零。

---

## 第 4 步：关闭公网注册（已内置）

注册接口 /api/register 已改为仅管理员可用（登录页只保留登录，管理员在「用户管理 → ＋ 添加用户」里手动创建账号）。未登录访问返回 401，普通用户访问返回 403。

加新账号：管理员在管理界面添加，或命令行 `python3 register.py 用户名 密码`（加 --admin 建管理员）。

验证：登录页无注册入口；非管理员调 /api/register 返回 403。

---

## 第 5 步：换 Gunicorn（必做）

Flask 开发服务器并发弱，公网会被拖垮。

```bash
pip install gunicorn
cd <项目路径> && gunicorn -w 4 -b 127.0.0.1:5001 run:app   # 先本机验证
```

`run:app` 即 run.py 里的 `app = create_app()`。之后别再用 `python3 run.py` 对外服务。

注册为 systemd 服务（开机自启 + 崩溃自动重启），创建 `/etc/systemd/system/babylog.service`：

```ini
[Unit]
Description=BabyLog Flask App
After=network.target

[Service]
User=<用户名>
WorkingDirectory=<项目路径>
ExecStart=<Python环境路径>/bin/gunicorn -w 4 -b 127.0.0.1:5001 run:app
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
```

ExecStart 的 python 路径以 `which gunicorn` 实际结果为准。

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now babylog
sudo systemctl status babylog
```

验证：`curl http://127.0.0.1:5001/login` 返回 200；重启后服务自动恢复。

---

## 第 6 步：Nginx 反向代理（必做）

由 Nginx 对外提供 80/443，隐藏后端 5001，同时承担静态文件、连接缓冲、访问日志。

```bash
sudo apt install nginx
```

创建 `/etc/nginx/sites-available/babylog`：

```nginx
server {
    listen 80;
    server_name _;              # 无域名用下划线

    client_max_body_size 4m;

    # 前端静态资源（可选，让 nginx 直接服务）
    location /static/ {
        alias <项目路径>/static/;
        expires 7d;
    }

    location / {
        proxy_pass http://127.0.0.1:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }
}
```

启用：

```bash
sudo ln -s /etc/nginx/sites-available/babylog /etc/nginx/sites-enabled/
sudo rm -f /etc/nginx/sites-enabled/default
sudo nginx -t
sudo systemctl reload nginx
```

验证：`curl http://127.0.0.1/` 返回 BabyLog 页面。

---

## 第 7 步：HTTPS（强烈建议）

没有 HTTPS 时，登录密码和会话 cookie 在公网明文传输。

### 方案 A：有域名（推荐，免费证书）
```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d 你的域名
```
证书到期自动续期由 certbot 定时任务处理。

### 方案 B：无域名（只能自签证书，浏览器会显示警告）
```bash
sudo mkdir -p /etc/nginx/certs && cd /etc/nginx/certs
sudo openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout babylog.key -out babylog.crt -subj "/CN=服务器IP"
```
再在 server 块加 443 监听，并把 80 的 server 改成 `return 301 https://$host$request_uri;`。手机首次访问需手动信任证书。

### 方案 C：不想暴露端口，用 Tailscale
```bash
curl -fsSL https://tailscale.com/install.sh | sh
sudo tailscale up
```
成员设备装 Tailscale 后通过 `http://机器名:5001` 访问，自带加密。

验证：https 能访问，F12 网络面板确认请求为 HTTPS。

---

## 第 8 步：数据库自动备份（已内置）

应用内已带自动备份，无需 crontab：

- 每天 03:30 备份到 `instance/backups/`（`babylog_时间戳.db`）
- 服务启动时立即备份一次
- 保留最近 30 天，自动清理；用 SQLite 在线备份，文件一致性有保证
- 配置：config.py 的 `BACKUP_ENABLED / BACKUP_DIR / BACKUP_TIME / BACKUP_RETENTION_DAYS`，可用环境变量 `BABYLOG_BACKUP_DIR` 等覆盖

验证：`ls instance/backups/` 能看到带时间戳的 .db；重启日志出现「[备份] 已备份」。

公网部署建议：把 backups 定期同步到对象存储（OSS / COS）做异地容灾，防磁盘损坏丢数据。

---

## 第 9 步：安全组 / 防火墙收口（必做）

公网端口暴露越少越好。

1. 云控制台安全组只放行：
   - 80/tcp（HTTP）来源 0.0.0.0/0
   - 443/tcp（HTTPS）来源 0.0.0.0/0
   - 22/tcp（SSH）来源仅你的 IP
   - 不放行 5001（后端已被 Nginx 反代，无需对外）
2. 服务器本机防火墙同步：

```bash
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow from 你的IP to any port 22
sudo ufw enable
```

验证：外网 `telnet 服务器IP 5001` 应失败，`telnet 服务器IP 80` 应成功。

---

## 第 10 步：收尾检查

- [ ] admin 已改为强密码
- [ ] SECRET_KEY 已通过环境变量设为随机值（非代码默认值）
- [ ] 登录限速生效（错 5 次锁 15 分钟，返回 429）
- [ ] 公网注册已关闭（仅管理员可加用户）
- [ ] Gunicorn + systemd 托管，开机自启
- [ ] Nginx 反代，`http://IP/` 可访问
- [ ] HTTPS 已启用（证书/自签/Tailscale 任选）
- [ ] 数据库每日备份已配置
- [ ] 安全组只开 80/443（+22 限 IP）
- [ ] 重启服务器后服务正常、数据未丢失

---

## 常见问题

**Q：改了代码后怎么重启？**
```bash
sudo systemctl restart babylog        # 若已用 systemd
# 或开发阶段：
cd <项目路径> && pkill -f "gunicorn|run.py"; python3 run.py
```

**Q：模板改了不生效？**
`debug=False` 下 Jinja2 有模板缓存，改 `templates/` 后必须重启服务；浏览器端如加载旧版，清一下 Service Worker 缓存（F12 → Application → 清除）。

**Q：想先在内网/局域网测整套方案？**
完全可以：Gunicorn + Nginx + 自签 HTTPS 在本机就能完整演练，把「云安全组」那步跳过即可，其余步骤照做。
