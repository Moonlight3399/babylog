# BabyLog 公网部署加固清单

> 适用场景：将 BabyLog 部署到有**公网 IP、无域名**的云服务器。
> 请**按编号顺序依次执行**，每完成一项打勾 ✅。改代码前先 `cp -r 项目路径 项目路径_backup_加固前` 留个备份。
>
> 涉及文件：`config.py`、`app/__init__.py`、`app/auth.py`、`run.py`、`register.py`（部署相关新增 systemd / nginx 配置文件）

---

## 现状核对（先了解当前状态）

| 检查点 | 当前代码状态 | 风险 |
|--------|-------------|------|
| Web 服务器 | `run.py` 用 Flask 开发服务器 `app.run()` | 高危 |
| HTTPS | 无，cookie `secure=False` 明文传输 | 高危 |
| 管理员密码 | 预置 `admin / admin` | 高危 |
| SECRET_KEY | `app/__init__.py` 每次启动 `secrets.token_hex(32)` 随机生成 | 中 |
| 登录限速 | 无 | 高危 |
| 注册接口 | `/api/register` 完全开放 | 中 |
| 数据库备份 | 无 | 中 |
| CSRF 防护 | 无 | 中 |

---

## 第 1 步：修改管理员默认密码 ✅（必须，5 分钟）

**风险**：`admin/admin` 暴露公网必被爆破。

**操作**：在服务器上执行（把 `新密码` 换成强密码）：

```bash
cd 项目路径 && python3 - <<'EOF'
from app import create_app, db, migrate_schema
from app.models import User
from app.auth import hash_password
app = create_app()
with app.app_context():
    migrate_schema(app)
    u = User.query.filter_by(username='admin').first()
    if u:
        u.password_hash = hash_password('新密码')
        db.session.commit()
        print('[✓] admin 密码已更新')
    else:
        print('[!] 未找到 admin 用户')
EOF
```

**验证**：用新密码登录 `http://服务器IP:5001` 成功，旧密码失败。

---

## 第 2 步：固定 SECRET_KEY（代码已内置默认值，部署时重新生成）✅（必须，3 分钟）

**状态**：代码已改为从 `config.py` 读取固定 SECRET_KEY（不再每次重启随机）。重启不再全员掉线，Gunicorn 多 worker 会话也能正常共享。

**部署到服务器时，请重新生成一个新密钥并覆盖默认值**（`config.py` 里的默认值仅供本地开发，公网部署必须更换）：

**① 在服务器上生成新密钥**：

```bash
python3 -c "import secrets; print(secrets.token_hex(32))"
```

**② 设置环境变量（推荐，不提交到 git）**：

```bash
export SECRET_KEY='粘贴上一步生成的值'    # 仅当前会话生效
# 永久生效：echo 'export SECRET_KEY="粘贴值"' >> ~/.bashrc
```

如果使用 systemd 托管（见第 5 步），在 `/etc/systemd/system/babylog.service` 的 `[Service]` 段加：

```ini
Environment=SECRET_KEY=粘贴上一步生成的值
```

**③ 重启服务验证**：重启后已登录用户仍保持登录（说明 SECRET_KEY 固定生效）。未设置环境变量时，自动使用代码内置默认值（本地开发可正常跑，但**公网部署必须覆盖**）。

> **迁移服务器**：想保留用户登录态就沿用同一个 SECRET_KEY（新服务器设置成同一个值）；想安全轮换（如旧 key 可能已泄露）就重新生成（用户需重新登录一次）。

---

## 第 3 步：登录加限速（防暴力破解）✅（已内置，无需操作）

**状态**：已在代码中实现**数据库持久化**的登录限速（不再是内存方案）：

- 同一「用户名 + IP」连续失败 **5 次**，锁定 **15 分钟**，期间返回 429「尝试次数过多」
- 失败记录存在 `login_attempts` 表，**跨进程（Gunicorn 多 worker）和重启后依然生效**，比内存方案更可靠
- 登录成功后自动清除失败记录；距离上次失败超过 15 分钟自动重新计数

**实现位置**：`app/models.py`（`LoginAttempt` 模型）+ `app/auth.py`（`_is_locked / _record_fail / _clear_fails` 及 `api_login` 集成）。

**验证**：连续输错 6 次密码，第 6 次起返回 429「尝试次数过多」；正确密码登录一次后，失败计数清零。

---

## 第 4 步：关闭公网注册 ✅（已内置，无需操作）

**状态**：注册接口 `/api/register` 已改为**仅管理员可用**（登录页只保留登录，管理员在"用户管理 → ＋ 添加用户"中手动创建账号）。未登录访问返回 401，普通用户访问返回 403。

> 需要加新账号时：管理员在管理界面添加，或命令行 `python3 register.py 用户名 密码`（或 `--admin`）。

**验证**：登录页无注册入口；非管理员调 `/api/register` 返回 403。

---

## 第 5 步：换用 Gunicorn 生产服务器 ✅（必须，15 分钟）

**风险**：Flask 开发服务器并发弱、无防护，公网会卡死/被 DoS。

**① 安装**：

```bash
pip install gunicorn
```

**② 用 Gunicorn 启动（先在本机验证，不要直接暴露公网端口）**：

```bash
cd 项目路径 && gunicorn -w 4 -b 127.0.0.1:5001 run:app
```

> `run:app` 即 `run.py` 里的 `app = create_app()`。之后不要再直接用 `python3 run.py` 对外提供服务。

**③ 注册为 systemd 服务（开机自启 + 崩溃自动重启）**：

创建 `/etc/systemd/system/babylog.service`：

```ini
[Unit]
Description=BabyLog Flask App
After=network.target

[Service]
User=aidlux
WorkingDirectory=项目路径
ExecStart=Python环境路径 -w 4 -b 127.0.0.1:5001 run:app
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
```

> `ExecStart` 的 python 路径以 `which gunicorn` 实际结果为准。

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now babylog
sudo systemctl status babylog
```

**验证**：`curl http://127.0.0.1:5001/login` 返回 200；`systemctl restart babylog` 后服务自动恢复。

---

## 第 6 步：Nginx 反向代理 ✅（必须，15 分钟）

**作用**：由 Nginx 对外提供 80/443，隐藏后端 5001；提供静态文件、连接缓冲、访问日志。

**① 安装**（若未装）：

```bash
sudo apt install nginx
```

**② 创建 `/etc/nginx/sites-available/babylog`**：

```nginx
server {
    listen 80;
    server_name _;              # 无域名用下划线

    client_max_body_size 4m;

    # 前端静态资源（可选，让 nginx 直接服务）
    location /static/ {
        alias 项目路径/static/;
        expires 7d;
    }

    location / {
        proxy_pass http://127.0.0.1:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }
}
```

**③ 启用并重启**：

```bash
sudo ln -s /etc/nginx/sites-available/babylog /etc/nginx/sites-enabled/
sudo rm -f /etc/nginx/sites-enabled/default   # 移除默认站点（可选）
sudo nginx -t
sudo systemctl reload nginx
```

**验证**：`curl http://127.0.0.1/` 返回 BabyLog 页面（走 80 端口）。

---

## 第 7 步：HTTPS 加密 ✅（强烈建议）

**风险**：无 HTTPS 时登录密码、会话 cookie 在公网明文传输，可被窃听。

### 方案 A：有域名（推荐，免费证书）
```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d 你的域名
```
证书到期自动续期由 certbot 定时任务处理。

### 方案 B：无域名（只能自签证书，浏览器会显示警告）
```bash
sudo mkdir -p /etc/nginx/certs && cd /etc/nginx/certs
sudo openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout babylog.key -out babylog.crt -subj "/CN=服务器IP"
```
然后在 `server` 块内加 443 监听并 `return 301 https://$host$request_uri;`（把 80 的 server 改成跳转）。手机首次访问需手动信任证书。

### 方案 C：更省心的替代——内网穿透（Tailscale）
```bash
curl -fsSL https://tailscale.com/install.sh | sh
sudo tailscale up
```
成员设备装 Tailscale 后用 `http://机器名:5001` 访问，自带加密，无需公网暴露端口。

**验证**：`https://` 访问成功；F12 网络面板确认请求为 HTTPS。

---

## 第 8 步：数据库自动备份 ✅（必须，10 分钟）

**风险**：SQLite 是单文件，服务器故障/误删=宝宝数据全丢。

**① 创建备份脚本 `/root/babylog_backup.sh`**：

```bash
#!/bin/bash
# BabyLog 数据库每日备份，保留 30 天
BACKUP_DIR=/root/babylog_backup
DB=项目路径/instance/babylog_new.db
mkdir -p "$BACKUP_DIR"
cp "$DB" "$BACKUP_DIR/babylog_$(date +%F_%H%M).db"
find "$BACKUP_DIR" -name 'babylog_*.db' -mtime +30 -delete
echo "[$(date)] backup done"
```

```bash
chmod +x /root/babylog_backup.sh
```

**② 加入 crontab（每天凌晨 3 点）**：

```bash
crontab -e
# 追加一行：
0 3 * * * /root/babylog_backup.sh >> /root/babylog_backup.log 2>&1
```

> 建议再把 `/root/babylog_backup/` 定期同步到对象存储（阿里云 OSS / 腾讯 COS）做异地容灾。

**验证**：手动执行一次脚本，`ls /root/babylog_backup/` 能看到带日期的 `.db` 文件。

---

## 第 9 步：云安全组 / 防火墙收口 ✅（必须，5 分钟）

**风险**：云服务器公网端口暴露越多，攻击面越大。

1. **云控制台「安全组」**：只放行
   - `80/tcp`（HTTP）来源 `0.0.0.0/0`
   - `443/tcp`（HTTPS，如有）来源 `0.0.0.0/0`
   - `22/tcp`（SSH）来源**仅限你的 IP**（重要！）
   - **不要**放行 `5001`（后端已被 Nginx 反代，无需对外）
2. 服务器本机防火墙（若启用）同步放行：

```bash
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow from 你的IP to any port 22
sudo ufw enable
```

**验证**：从外网 `telnet 服务器IP 5001` 应失败/超时；`telnet 服务器IP 80` 应成功。

---

## 第 10 步：收尾检查清单 ✅

- [ ] 第 1 步：admin 强密码已改
- [ ] 第 2 步：SECRET_KEY 已固定为强随机值
- [ ] 第 3 步：登录限速已生效（错 6 次被锁）
- [ ] 第 4 步：（可选）公网注册已关闭
- [ ] 第 5 步：Gunicorn + systemd 托管，开机自启
- [ ] 第 6 步：Nginx 反代，`http://IP/` 可访问
- [ ] 第 7 步：HTTPS 已启用（证书/自签/Tailscale 任选）
- [ ] 第 8 步：数据库每日备份已配置
- [ ] 第 9 步：安全组只开 80/443(+22限IP)
- [ ] 重启服务器后 `systemctl status babylog` 正常、数据未丢失

---

## 常见问题

**Q：改了代码后怎么重启？**
```bash
sudo systemctl restart babylog        # 若已用 systemd
# 或开发阶段：
cd 项目路径 && pkill -f "gunicorn|run.py"; python3 run.py
```

**Q：模板改了不生效？**
`debug=False` 下 Jinja2 有模板缓存，改 `templates/` 后必须重启服务；浏览器端如加载旧版，清一下 Service Worker 缓存（F12 → Application → 清除）。

**Q：想先在内网/局域网测整套方案？**
完全可以：Gunicorn + Nginx + 自签 HTTPS 在本机就能完整演练，把「云安全组」那步跳过即可，其余步骤照做。
